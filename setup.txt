you must have the following python modules:
- pygame
- numpy
- pillow

only run setup.bat if you have python installed
make sure to also have venv installed on python

pip install venv

run setup.bat in the directory you want the venv to be made in